#pragma once

namespace CobbPatches {
   namespace Miscellaneous {
      namespace NativeResolution {}; // Force the UI to render at the native resolution rather than a normalized/scaled one
      //
      void Apply();
   };
};